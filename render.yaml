# Render Blueprint - optional. Or set these manually in Dashboard:
# Build Command: npm run build
# Start Command: npm run start

services:
  - type: web
    name: ai-exam-assistant
    env: node
    buildCommand: npm run build
    startCommand: npm run start
